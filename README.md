# junit2json-rust
JUnit XML format to JSON with Rust

# Test
```bash
# Run
cargo nextest run

# Update snapshot
## Need to install cargo-insta first.
## `cargo install cargo-insta`
cargo insta review
```
